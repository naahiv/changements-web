// components
import SectionContainer from '../SectionContainer'

const Verification = () => {
	return (
		<SectionContainer marginTop={true} title={'Verification'}>
			<p style={{ gridColumn: 'span 12' }}>Please verify your email address.</p>
		</SectionContainer>
	)
}

export default Verification
